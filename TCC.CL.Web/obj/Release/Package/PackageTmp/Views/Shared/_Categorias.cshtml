@model List<string>
<h4>Categorias</h4>
<div style="text-align:center;">
    @if (Model.Count() > 0)
    {
        foreach (var cat in Model)
        {
            <a href="@Url.Action("Index", "Busca", new { q = cat })" class="badge">@cat</a>
        }
    }
</div>